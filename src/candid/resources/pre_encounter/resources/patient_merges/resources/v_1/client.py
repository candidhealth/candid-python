# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from .......core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .......core.request_options import RequestOptions
from ....common.types.patient_merge_id import PatientMergeId
from .raw_client import AsyncRawV1Client, RawV1Client
from .types.mutable_patient_merge import MutablePatientMerge
from .types.patient_merge import PatientMerge
from .types.patient_merge_status import PatientMergeStatus

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class V1Client:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawV1Client(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawV1Client:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawV1Client
        """
        return self._raw_client

    def create(
        self, *, request: MutablePatientMerge, request_options: typing.Optional[RequestOptions] = None
    ) -> PatientMerge:
        """
        Creates a new patient merge record.

        Parameters
        ----------
        request : MutablePatientMerge

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PatientMerge

        Examples
        --------
        from candid import CandidApiClient
        from candid.resources.pre_encounter.resources.patient_merges.resources.v_1 import (
            MutablePatientMerge,
        )

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pre_encounter.patient_merges.v_1.create(
            request=MutablePatientMerge(
                alternative_patient_mrn="alternative_patient_mrn",
                primary_patient_mrn="primary_patient_mrn",
            ),
        )
        """
        _response = self._raw_client.create(request=request, request_options=request_options)
        return _response.data

    def get_status(
        self, mrn_or_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PatientMergeStatus:
        """
        Gets the merge status for a patient by patient ID or mrn. If the provided value is a valid UUID, it will be treated as a patient ID. Otherwise, it will be treated as an MRN.

        Parameters
        ----------
        mrn_or_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PatientMergeStatus

        Examples
        --------
        from candid import CandidApiClient

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pre_encounter.patient_merges.v_1.get_status(
            mrn_or_id="mrn_or_id",
        )
        """
        _response = self._raw_client.get_status(mrn_or_id, request_options=request_options)
        return _response.data

    def get_all_by_mrn(
        self, mrn: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[PatientMerge]:
        """
        Gets all patient merge records that have the given mrn.

        Parameters
        ----------
        mrn : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PatientMerge]

        Examples
        --------
        from candid import CandidApiClient

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pre_encounter.patient_merges.v_1.get_all_by_mrn(
            mrn="mrn",
        )
        """
        _response = self._raw_client.get_all_by_mrn(mrn, request_options=request_options)
        return _response.data

    def deactivate(
        self, id: PatientMergeId, version: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deactivates a patient merge record. Path must contain next version.

        Parameters
        ----------
        id : PatientMergeId

        version : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from candid import CandidApiClient

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pre_encounter.patient_merges.v_1.deactivate(
            id="id",
            version="version",
        )
        """
        _response = self._raw_client.deactivate(id, version, request_options=request_options)
        return _response.data

    def scan(
        self,
        *,
        since: dt.datetime,
        max_results: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[PatientMerge]:
        """
        Scans up to 1000 patient merge updates. The since query parameter is inclusive, and the result list is ordered by updatedAt ascending.

        **Polling Pattern:**
        To continuously poll for updates without gaps:
        1. Make your initial request with a `since` timestamp (e.g., `since=2020-01-01T13:00:00.000Z`)
        2. The API returns 100 by default and up to 1000 patient merge records, sorted by `updated_at` ascending
        3. Find the `updated_at` value from the last record in the response
        4. Use that `updated_at` value as the `since` parameter in your next request
        5. Repeat steps 2-4 to ingest updates until you receive an empty list

        **Important Notes:**
        - The `since` parameter is inclusive, so you may receive the last record from the previous batch again (you can deduplicate by ID and version)
        - All patient merge records include `updated_at`, `id`, `version`, `deactivated`, and `updating_user` fields for tracking changes
        - Timestamps have millisecond resolution for precise ordering

        Parameters
        ----------
        since : dt.datetime

        max_results : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PatientMerge]

        Examples
        --------
        import datetime

        from candid import CandidApiClient

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.pre_encounter.patient_merges.v_1.scan(
            since=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.scan(since=since, max_results=max_results, request_options=request_options)
        return _response.data


class AsyncV1Client:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawV1Client(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawV1Client:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawV1Client
        """
        return self._raw_client

    async def create(
        self, *, request: MutablePatientMerge, request_options: typing.Optional[RequestOptions] = None
    ) -> PatientMerge:
        """
        Creates a new patient merge record.

        Parameters
        ----------
        request : MutablePatientMerge

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PatientMerge

        Examples
        --------
        import asyncio

        from candid import AsyncCandidApiClient
        from candid.resources.pre_encounter.resources.patient_merges.resources.v_1 import (
            MutablePatientMerge,
        )

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pre_encounter.patient_merges.v_1.create(
                request=MutablePatientMerge(
                    alternative_patient_mrn="alternative_patient_mrn",
                    primary_patient_mrn="primary_patient_mrn",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(request=request, request_options=request_options)
        return _response.data

    async def get_status(
        self, mrn_or_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PatientMergeStatus:
        """
        Gets the merge status for a patient by patient ID or mrn. If the provided value is a valid UUID, it will be treated as a patient ID. Otherwise, it will be treated as an MRN.

        Parameters
        ----------
        mrn_or_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PatientMergeStatus

        Examples
        --------
        import asyncio

        from candid import AsyncCandidApiClient

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pre_encounter.patient_merges.v_1.get_status(
                mrn_or_id="mrn_or_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_status(mrn_or_id, request_options=request_options)
        return _response.data

    async def get_all_by_mrn(
        self, mrn: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[PatientMerge]:
        """
        Gets all patient merge records that have the given mrn.

        Parameters
        ----------
        mrn : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PatientMerge]

        Examples
        --------
        import asyncio

        from candid import AsyncCandidApiClient

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pre_encounter.patient_merges.v_1.get_all_by_mrn(
                mrn="mrn",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_all_by_mrn(mrn, request_options=request_options)
        return _response.data

    async def deactivate(
        self, id: PatientMergeId, version: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deactivates a patient merge record. Path must contain next version.

        Parameters
        ----------
        id : PatientMergeId

        version : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from candid import AsyncCandidApiClient

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pre_encounter.patient_merges.v_1.deactivate(
                id="id",
                version="version",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deactivate(id, version, request_options=request_options)
        return _response.data

    async def scan(
        self,
        *,
        since: dt.datetime,
        max_results: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[PatientMerge]:
        """
        Scans up to 1000 patient merge updates. The since query parameter is inclusive, and the result list is ordered by updatedAt ascending.

        **Polling Pattern:**
        To continuously poll for updates without gaps:
        1. Make your initial request with a `since` timestamp (e.g., `since=2020-01-01T13:00:00.000Z`)
        2. The API returns 100 by default and up to 1000 patient merge records, sorted by `updated_at` ascending
        3. Find the `updated_at` value from the last record in the response
        4. Use that `updated_at` value as the `since` parameter in your next request
        5. Repeat steps 2-4 to ingest updates until you receive an empty list

        **Important Notes:**
        - The `since` parameter is inclusive, so you may receive the last record from the previous batch again (you can deduplicate by ID and version)
        - All patient merge records include `updated_at`, `id`, `version`, `deactivated`, and `updating_user` fields for tracking changes
        - Timestamps have millisecond resolution for precise ordering

        Parameters
        ----------
        since : dt.datetime

        max_results : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PatientMerge]

        Examples
        --------
        import asyncio
        import datetime

        from candid import AsyncCandidApiClient

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.pre_encounter.patient_merges.v_1.scan(
                since=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.scan(since=since, max_results=max_results, request_options=request_options)
        return _response.data
