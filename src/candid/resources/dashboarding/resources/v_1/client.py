# This file was auto-generated by Fern from our API Definition.

import typing

from .....core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .....core.request_options import RequestOptions
from .raw_client import AsyncRawV1Client, RawV1Client
from .types.metric_query import MetricQuery
from .types.metrics_query_response import MetricsQueryResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class V1Client:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawV1Client(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawV1Client:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawV1Client
        """
        return self._raw_client

    def query_metrics(
        self, *, request: typing.Sequence[MetricQuery], request_options: typing.Optional[RequestOptions] = None
    ) -> MetricsQueryResponse:
        """
        Retrieves values for one or more metrics. Each metric is computed independently, so a
        metric that cannot be resolved returns an `error` on its own result rather than failing
        the request.

        Each query reads current state by default. Count metrics also accept a `yesterday`
        snapshot, so day-over-day change is computed by querying both and taking the difference.

        See the [Metrics Reference](/docs/dashboarding-metrics) for the full catalog, including
        value types and staleness SLAs.

        Parameters
        ----------
        request : typing.Sequence[MetricQuery]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MetricsQueryResponse

        Examples
        --------
        from candid import CandidApiClient
        from candid.resources.dashboarding.resources.v_1 import MetricName, MetricQuery

        client = CandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.dashboarding.v_1.query_metrics(
            request=[
                MetricQuery(
                    metric=MetricName.PATIENT_TOTAL_BALANCE_V_1,
                ),
                MetricQuery(
                    metric=MetricName.PATIENT_TOTAL_BALANCE_V_1,
                ),
            ],
        )
        """
        _response = self._raw_client.query_metrics(request=request, request_options=request_options)
        return _response.data


class AsyncV1Client:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawV1Client(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawV1Client:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawV1Client
        """
        return self._raw_client

    async def query_metrics(
        self, *, request: typing.Sequence[MetricQuery], request_options: typing.Optional[RequestOptions] = None
    ) -> MetricsQueryResponse:
        """
        Retrieves values for one or more metrics. Each metric is computed independently, so a
        metric that cannot be resolved returns an `error` on its own result rather than failing
        the request.

        Each query reads current state by default. Count metrics also accept a `yesterday`
        snapshot, so day-over-day change is computed by querying both and taking the difference.

        See the [Metrics Reference](/docs/dashboarding-metrics) for the full catalog, including
        value types and staleness SLAs.

        Parameters
        ----------
        request : typing.Sequence[MetricQuery]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MetricsQueryResponse

        Examples
        --------
        import asyncio

        from candid import AsyncCandidApiClient
        from candid.resources.dashboarding.resources.v_1 import MetricName, MetricQuery

        client = AsyncCandidApiClient(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.dashboarding.v_1.query_metrics(
                request=[
                    MetricQuery(
                        metric=MetricName.PATIENT_TOTAL_BALANCE_V_1,
                    ),
                    MetricQuery(
                        metric=MetricName.PATIENT_TOTAL_BALANCE_V_1,
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.query_metrics(request=request, request_options=request_options)
        return _response.data
